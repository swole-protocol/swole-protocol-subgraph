{
  "name": "swole-protocol-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.thegraph.com/deploy/ mnlesane/swole-protocol-subgraph",
    "create-local": "graph create --node http://localhost:8020/ mnlesane/swole-protocol-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ mnlesane/swole-protocol-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 mnlesane/swole-protocol-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.30.3",
    "@graphprotocol/graph-ts": "0.27.0"
  }
}
